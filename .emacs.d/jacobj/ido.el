;; enable IDO mode
(require 'ido)
(ido-mode t)

;; ;; remap keybindings
;; (defun bind-ido-keys ()
;;   "Keybindings for ido mode."
;; 	(define-key ido-completion-map (kbd "M-b") ' ido-switch-buffer))

;; (add-hook 'ido-setup-hook #'bind-ido-keys)
